# Install Archiver Package

To enable ZIP download functionality, install the archiver package:

```bash
cd backend
npm install archiver
npm install --save-dev @types/archiver
```

This package is required for:
- Downloading multiple images as ZIP
- Downloading entire folders as ZIP archives
