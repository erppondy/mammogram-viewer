interface LicenseExpiryNoticeProps {
  ambulanceName: string;
}

export default function LicenseExpiryNotice({
  ambulanceName,
}: LicenseExpiryNoticeProps) {
  // Expiry feature removed - this component is now a placeholder
  return null;
}
